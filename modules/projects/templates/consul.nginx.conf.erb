server {
  access_log <%= scope.lookupvar "nginx::config::logdir" %>/consul.access.log main;
  listen 80;
  server_name devconsul.daptiv.com;
  ignore_invalid_headers off;

  location / {
    proxy_pass http://localhost:8500;
  }

}
