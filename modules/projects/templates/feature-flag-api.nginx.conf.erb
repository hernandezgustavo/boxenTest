server {
  access_log <%= scope.lookupvar "nginx::config::logdir" %>/feature-flag-api.access.log main;
  listen 80;
  server_name devflag.daptiv.com;
  ignore_invalid_headers off;

  location / {
    proxy_pass http://localhost:8082;
  }

}
