server {
  access_log <%= scope.lookupvar "nginx::config::logdir" %>/teammember-client.access.log main;
  listen 80;
  server_name tm.daptiv.com;
  ignore_invalid_headers off;

  client_max_body_size 50M;

  location / {
    proxy_pass http://unix:/tmp/tm-client.sock;
  }

}
